# Host port used by Docker Compose.
HARBOR_PORT=7750

# Optional: protect edits with an administrator password.
# Viewing and opening services remain available without signing in.
# Set this before exposing Harbor beyond a trusted home network.
HARBOR_ADMIN_PASSWORD=
